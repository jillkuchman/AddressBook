<h3>Here are all the contacts in your specified category:</h3>
<ol>
@foreach (var task in @Model["tasks"])
{
  <li>@task.GetDescription()</li>
}
</ol>
<p><a href='/categories/@Model["category"].GetId()/tasks/new'>Add a new contact here</a></p>
<p><a href='/'>Return to Main Page</a></p>
